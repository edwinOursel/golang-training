package multiply

// create a function to multiply two positive numbers using a loop and the add function (not the '*' sign)
// when it works, make sure the add fuction sends a panic for a negative parameter and the multiply function handles it correctly
